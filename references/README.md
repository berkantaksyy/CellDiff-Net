This folder contains *generated* reference artifacts.

- `papers.json`: structured metadata per paper
- `references.bib`: BibTeX export

Update by running:

```bash
python3 ../tools/update_references.py
```

Note: paper PDFs should remain under `../private/papers/` and are intentionally ignored by git.
